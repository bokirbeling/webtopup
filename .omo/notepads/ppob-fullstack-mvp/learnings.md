# Learnings

- Task 15 runbook now replaces the old high-level README with a localhost-first operational guide covering environment groups, install/dev commands, verification commands, webhook simulation fixtures, troubleshooting, evidence files, and deferred Adnanpay deployment. `.env.example` uses the same group order as README and placeholder-only values.

- Task 14 regression suite now has deterministic backend lifecycle coverage in `backend/src/modules/regression/lifecycle.regression.test.ts`: local order `ORD-TEST-001` / invoice `INV-TEST-0001`, mocked Midtrans init, signed Midtrans callback `TX-TEST-001`, Digiflazz callback `DGF-TEST-001`, invoice success lookup, idempotent replay (`idempotent: true`), signature failure no-mutation, and reconciliation out-of-order/denied cases.
- Task 14 frontend regression remains Vitest-based because Playwright is not installed; browser evidence screenshot was produced with installed headless Chrome using deterministic local HTML evidence after Vite headless capture showed a blank page in this environment.

- Task 1 scaffold uses a minimal `backend/` TypeScript workspace (`src/index.ts`, `tsconfig.json`, `eslint.config.mjs`, package scripts) to unblock Express bootstrap in the next task.
- Security hygiene: root `.env.example` intentionally uses placeholder-only values (`your_value_here`) for Supabase, Midtrans, and Digiflazz keys.
- Existing `.env` and `Frontend/webhook.md` contain sensitive legacy-like values, so verification was done with explicit pattern scans to ensure none were copied into `.env.example`.
- Task 2 bootstrap pattern works cleanly with current NodeNext+strict setup by keeping runtime code under `backend/src/` (`config/env.ts`, `app.ts`, `routes/health.ts`, `index.ts`) and preserving existing script conventions.
- Explicit `REQUIRED_ENV_KEYS` + typed parsing (`NODE_ENV`, `PORT`) gives deterministic fail-fast startup behavior and keeps `/health` independent from business services.
- Scope-boundary recovery for Task 2 is reliable by resetting the index, restaging only intended files, and adding root ignore rules for `backend/node_modules/` and `backend/dist/`.
- Supabase Task 3 scaffold is created safely with `supabase init`, which generates `supabase/config.toml` plus local temp ignore defaults without introducing schema SQL.
- Migration workflow readiness can be validated at command level using `supabase migration --help`, `supabase migration new --help`, `supabase migration list --help`, and `supabase db --help` before any schema execution.
- Extending `backend/src/config/env.ts` with `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` keeps startup fail-fast and keeps Supabase config binding centralized in existing strict env parsing.
- Task 4 baseline works with a single migration that defines only the five canonical transaction entities (`orders`, `payments`, `fulfillments`, `webhook_events`, `status_history`) and keeps all status domains explicit via CHECK constraints.
- Idempotency can be enforced at DB level with unique constraints on provider-level keys (`payments(provider, idempotency_key)` and `webhook_events(provider, event_key)`), with nullable external IDs guarded by partial unique indexes.
- For MVP safety, transaction-critical tables should run with RLS enabled+forced and only `service_role` policies so anon/authenticated clients cannot write directly.
- Monotonic-safe transition checks are practical at schema level by recording `status_domain` in `status_history` and enforcing forward-only rank checks plus terminal-state lock constraints in CHECK expressions.
- Keeping one canonical Task 4 migration file avoids duplicated table definitions and keeps downstream tasks (`7/8/9/11`) aligned to stable table names (`fulfillments`, not `fulfillment_attempts`).
- Task 4 final schema must use exact entity names from the plan (`fulfillment_attempts` instead of `fulfillments`) or downstream task contracts become inconsistent.
- Local verification can prove both security and idempotency directly at DB level: `SET ROLE anon` insert attempts are denied, and duplicate `(provider, event_key)` inserts are blocked by unique constraints while keeping a single stored event.
- `supabase db lint --local` and `supabase db advisors --local` are fast post-migration checks to catch schema safety/performance regressions before backend integration tasks.
- Canonical Task 4 migration now uses exact entity names required by orchestrator scope: `orders`, `payments`, `fulfillments`, `webhook_events`, and `status_history` in one SQL file.
- Monotonic-safety can be enforced at DB layer for audit rows via rank-based `CHECK` on `status_history` (`from_status` rank <= `to_status` rank), while still allowing terminal statuses (`success|failed|expired`) as same-rank endpoints.
- Provider-side dedupe resilience is stronger when nullable external references use partial unique indexes in addition to mandatory idempotency keys (`payments(provider,idempotency_key)`, `webhook_events(provider,event_key)`).
- Stale-artifact check should verify actual file content before deletion; in current state `20260422010406_transactional_schema_rls_idempotency_task4_v2.sql` is the only migration file and contains the canonical Task 4 SQL.
- Task 4 stale-cleanup verification should treat deletion as conditional: when the reported file is SQL-bearing canonical content, cleanup becomes a no-op and verification proceeds with migration command evidence.
- Recreating Task 4 via `supabase migration new transactional_schema_rls_idempotency_task4_v2` keeps migration provenance aligned with CLI workflow while preserving a single canonical migration file in `supabase/migrations/`.
- Monotonic safety is stricter when rank ordering is combined with a terminal-state lock check in `status_history`, so terminal statuses (`success|failed|expired`) cannot transition to a different terminal value.
- Task 5 backend test bootstrap stays deterministic by testing `createApp()` in-memory with Supertest (`GET /health`) and asserting status code, response body, and JSON content-type.
- Existing frontend workspace already uses `vitest.config.ts` + `src/test/app.smoke.test.tsx`; keeping smoke tests local to jsdom avoids external dependencies while satisfying minimal app render coverage.
- Task 5 backend test harness can stay isolated from runtime code by using `jest.config.cjs` + `tsconfig.test.json` and a `backend/test/health.test.ts` Supertest smoke case that targets `createApp()` directly.
- With `module: NodeNext`, `ts-jest` warns unless test transpilation uses `isolatedModules`; setting it in `backend/tsconfig.test.json` removes noisy warnings while keeping app `tsconfig.json` unchanged.
- Frontend smoke coverage is stable with a dedicated `vitest.config.ts` (`jsdom`, setup file, CSS enabled) plus a single deterministic render assertion in `src/test/app.smoke.test.tsx` against visible hero CTA content.
- Task 6 CI gate mapping is directly script-compatible across workspaces: `Frontend` and `backend` both expose `lint`, `typecheck`, `test`, and `build`, so workflow steps can use `npm run <script>` consistently.
- A strict two-job workflow (`frontend-quality`, `backend-quality`) with default step behavior (no `continue-on-error`) is sufficient for fail-fast quality gates on push and pull requests.
- Final Task 6 scope retained the pre-existing canonical workflow `.github/workflows/ci-quality-gates.yml` (single `quality-gates` job with explicit backend+frontend gate steps) and removed duplicate workflow files to avoid redundant CI runs.
- Task 6 CI quality gates can stay deterministic with one workflow job that installs each workspace via `npm ci --prefix <workspace>` and then runs explicit gate steps (`lint`, `typecheck`, `test`, `build`) so failures propagate immediately by default GitHub Actions step behavior.
- Task 7 order API can stay deterministic and testable by injecting `idGenerator`, `invoiceCodeGenerator`, and `clock` into the order service while keeping production defaults (`randomUUID`, timestamped invoice code).
- Enforcing server-controlled fields (`status`, `order_number`, `invoice_code`, `order_id`, `id`) at payload-validation level prevents clients from bypassing the canonical `pending_payment` initialization path.
- A rank-based transition engine (`created -> pending_payment -> paid -> fulfillment_pending -> success|failed|expired`) combined with status-history writes on create/transition provides monotonic behavior with an auditable `status_history` timeline.
- Housekeeping cleanup: removed obsolete plan file `.sisyphus/plans/runtime-model-timeout-unblock.md` on user request; verified `.sisyphus/plans/` still contains `ppob-fullstack-mvp.md`.
- Task 7 verification is strongest when evidenced by direct runtime probes: one API-level create probe asserting `201` + `pending_payment` response plus initial `status_history` row, and one service-level forbidden transition probe asserting `OrderTransitionError` for backward `paid -> pending_payment`.
- Task 8 Midtrans integration is stable when payment initialization and webhook handling are separated into a dedicated payment module (`payment.repository.ts`, `payment.service.ts`, `payment.router.ts`) and wired via `createApp()` so order transitions still flow through the canonical `orderService.transitionOrderStatus` monotonic guard.
- Webhook safety pattern for Midtrans: enforce signature check (`sha512(order_id + status_code + gross_amount + server_key)`) before any persistence/mutation, then persist dedupe key on `webhook_events(provider,event_key)` and short-circuit replay as `DUPLICATE` so status transitions apply exactly once.
- Evidence workflow for Task 8 can reuse Task 7 rigor by running API probes with deterministic in-memory dependencies and saving outputs under `.sisyphus/evidence/` (`task-8-midtrans-idempotent.txt`, `task-8-midtrans-signature-error.txt`) to prove both happy-path idempotency and signature rejection behavior.
- Task 8 Midtrans webhook processing should verify `signature_key = sha512(order_id + status_code + gross_amount + MIDTRANS_SERVER_KEY)` before any DB idempotency registration or order/payment mutation.
- Task 8 keeps webhook replay idempotency on `webhook_events(provider,event_key)` and confirms replay does not create another `status_history` transition.

- Task 9 fulfillment uses the canonical ulfillments table and shared webhook_events provider/event_key dedupe, with order status changes routed only through OrderService.transitionOrderStatus. Digiflazz mock fallback is recorded via provider_mode=mock in fulfillment payloads and is guarded off in production.
- Task 10 browser evidence was captured against Vite at http://127.0.0.1:5173 with Playwright-side mocked POST /api/orders and /api/payments/midtrans/initialize responses, avoiding live Supabase/backend dependencies while proving visible invoice/status and error/retry states.
- Task 12 security hardening uses a dependency-injected `AuditLogger` plus local in-memory rate-limit middleware on sensitive payment/fulfillment routes, keeping audit entries sanitized to route/method/status/reason/provider/order id and never logging provider keys or Supabase service credentials.
- Task 12 production Digiflazz guard remains enforced in the fulfillment service (`NODE_ENV=production` without credentials throws `Digiflazz credentials are required in production.`), and the route now records a sanitized `fulfillment_security_rejection` audit event while creating no mock fulfillment.
- Task 13 reconciliation is implemented as a deterministic backend service under `backend/src/modules/reconcile/**`; it detects payment/fulfillment-derived inconsistencies and only corrects order status via `OrderService.transitionOrderStatus`, returning explicit transitioned/denied/error result objects with audit-style metadata.
- Task 13 successful fulfillment reconciliation may require two safe monotonic steps (`paid -> fulfillment_pending -> success`) when fulfillment success arrives before the order recorded fulfillment start.
