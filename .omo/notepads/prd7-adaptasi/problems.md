# Problems
- Mismatch between order creation status ("created") vs expected status ("pending_payment") in automated tests must be handled gracefully or fixed to establish correct baseline verification.
