Task 1 legacy secret scan evidence

Scope:
- Documentation and `.env.example` were checked to ensure no live/legacy secret values from local `.env` or `Frontend/webhook.md` were copied.
- `.env` has been removed from the git index with `git rm --cached -f -- .env` and is ignored via `.gitignore`.

Result: PASS. No legacy secret exposure is required for tracked docs/templates; local `.env` remains local-only.
