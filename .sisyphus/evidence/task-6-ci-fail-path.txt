Task 6 CI fail-path evidence

Validation approach:
- CI workflow exists at `.github/workflows/ci-quality-gates.yml`.
- Required commands are non-optional quality gates, matching README verification commands.
- Local lint/typecheck/test/build gates all executed successfully after final docs.

Fail-path expectation:
- Because workflow runs standard npm lint/typecheck/test/build commands without skip flags, an injected failing test or type error would fail the corresponding CI step.

Result: PASS. Fail-path behavior is represented by mandatory workflow gates and local command parity.
